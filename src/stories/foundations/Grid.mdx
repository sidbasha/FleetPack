import { Meta } from '@storybook/addon-docs/blocks';

<Meta title="Foundations/Grid &#38; Layout" />

# Grid & Layout

Desktop-first, built for fixed workstation monitors and ultra-wide fab
displays.

## Breakpoints

| Breakpoint | Min width | Token | Behavior |
|---|---|---|---|
| `compact` | 1024px | `--breakpoint-compact` | Nav rail collapses to icon-only; table toolbars wrap to two rows; minimum supported width for any FleetPack page |
| `standard` | 1280px | `--breakpoint-standard` | Default target: 264px nav rail + content, 12-column content grid, 24px gutter |
| `wide` | 1600px | `--breakpoint-wide` | Dashboards move from 1 to 2 KPI-panel columns; tables gain side margins |
| `ultra-wide` | 1920px+ | `--breakpoint-ultrawide` | Content max-width caps at 1600px and centers, so a table never stretches full-bleed on a fab-floor ultra-wide |

These are additive Tailwind breakpoint names (`compact:`, `standard:`,
`wide:`, `ultrawide:`) — the framework's own `sm`/`md`/`lg`/`xl`/`2xl` are
untouched, since existing components already key off them.

## Base grid, columns & gutters

A 12-column grid underlies every FleetPack page. Components span whole
columns; nothing is positioned at an arbitrary fractional width.

| Property | Value | Rule |
|---|---|---|
| Columns | 12 | Standard & wide breakpoints; compact collapses to 4; ultra-wide keeps 12 within the capped container |
| Gutter | 24px | `sp-6`, fixed regardless of breakpoint |
| Margin | 64px / 24px | Content edge to viewport edge: `sp-16` at standard/wide, `sp-6` at compact |
| Min column width | 64px | Below this a component should span 2+ columns rather than shrink further |

## Containers: fixed & fluid

| Container | Behavior | Used for |
|---|---|---|
| Fluid | Width tracks the viewport between breakpoints, gutters and margins fixed | Page shell, table wrappers, dashboards |
| Fixed-capped | Fluid until 1600px, then centers with equal margins | Page content on ultra-wide fab monitors |
| Fixed | A hard pixel width regardless of viewport | Modals (360/440/640px), drawers (340–480px) |

## Dashboard & card grids

KPI rows and card grids intentionally use explicit column counts rather than
the full 12, favoring legibility over mathematical purity: a KPI row uses
3–4 explicit columns (2 at compact), a module dashboard uses 1 panel-per-column
at standard and 2 at wide with each panel independently sized to its own
content rather than a fixed grid cell, and a card grid uses 3 columns
standard / 2 compact with a 280px minimum card width before reflowing.

## KPI rows vs. card grids

They look alike — a row of small rectangles — and answer different questions.

| Question | KPI row | Card grid |
|---|---|---|
| What is a tile? | Fixed measure, one subject | One object, with several attributes |
| How many? | Fixed — 3 or 4, decided at design time | Variable, driven by the data, paginated past 24 |
| Does the set change? | No. Same tiles, same order, every load | Yes. Filtering and sorting reorder it |
| Is a tile clickable? | Optional — drills into that metric | Yes, the card *is* the object's entry point |
| Empty behavior | Tile stays, shows an em-dash and a reason | Grid collapses to an empty state |
| Where it sits | Top of a page, above what it summarizes | The page's main content |
| Columns | 3–4 standard and wide, 2 compact | 3 standard, 2 compact, min card 280px |

<div style={{ display: 'grid', gridTemplateColumns: 'repeat(auto-fit,minmax(260px,1fr))', gap: '16px', margin: '16px 0' }}>
  <div style={{ border: '1px solid var(--color-success)', background: 'var(--color-success-surface)', borderRadius: '8px', padding: '14px' }}>
    <div style={{ fontSize: '11px', fontWeight: '700', color: 'var(--color-success-hover)', marginBottom: '8px' }}>✓ DO</div>
    <div style={{ display: 'flex', gap: '16px' }}>
      <div><div style={{ fontFamily: 'var(--font-mono)', fontWeight: '700', fontSize: '18px', color: 'var(--color-ink-900)' }}>96.4%</div><div style={{ fontSize: '10px', color: 'var(--color-ink-500)' }}>UP-TIME</div></div>
      <div><div style={{ fontFamily: 'var(--font-mono)', fontWeight: '700', fontSize: '18px', color: 'var(--color-ink-900)' }}>2.1h</div><div style={{ fontSize: '10px', color: 'var(--color-ink-500)' }}>MTTR</div></div>
    </div>
    <p style={{ fontSize: '12px', color: 'var(--color-ink-600)', margin: '10px 0 0' }}>A fixed row of measures the fleet page is already about.</p>
  </div>
  <div style={{ border: '1px solid var(--color-error)', background: 'var(--color-error-surface)', borderRadius: '8px', padding: '14px' }}>
    <div style={{ fontSize: '11px', fontWeight: '700', color: 'var(--color-error-text)', marginBottom: '8px' }}>✗ DON'T</div>
    <div style={{ display: 'flex', gap: '16px' }}>
      <div><div style={{ fontFamily: 'var(--font-mono)', fontWeight: '700', fontSize: '14px', color: 'var(--color-ink-900)' }}>SP7-04</div><div style={{ fontSize: '10px', color: 'var(--color-ink-500)' }}>98.4%</div></div>
      <div><div style={{ fontFamily: 'var(--font-mono)', fontWeight: '700', fontSize: '14px', color: 'var(--color-ink-900)' }}>EDR-11</div><div style={{ fontSize: '10px', color: 'var(--color-ink-500)' }}>91.2%</div></div>
    </div>
    <p style={{ fontSize: '12px', color: 'var(--color-ink-600)', margin: '10px 0 0' }}>Put one object per KPI tile — that's a collection, and 214 tools won't fit in a row.</p>
  </div>
</div>

## Common page structures

Eight structures cover FleetPack's surface today, and the shapes any new
module reaches for.

| Structure | Use when | Grid | Watch for |
|---|---|---|---|
| Dashboard | The operator is checking whether anything needs attention | KPI row up 4, panels span 8/4 | Panels that require scrolling before the first chart is visible |
| Detail page | One object is the subject and the operator arrived here deliberately | span 8 content, span 4 rail | Burying the object's identity |
| Table page | Filter toolbar, grid, then a footer holding the range, page size and pager | Full width, inner scroll | Page-level horizontal scroll |
| Split pane | Working through a list where each item needs inspection, alarms, events, results | List 320–400px, detail fills | Below 1024px the pane stacks and the detail becomes a drawer |
| Wizard | A task with a required order the operator completes in one sitting | Span 6–8, centered | More than six steps: group them, or let the operator leave and return via a checklist |
| Comparison | Two to four subjects judged against the same measures | 2–4 equal columns | Five or more columns — that's a table |
| First run | A module with no data yet, or a filter that returns nothing | Span 4–6, centered | More than one primary action; the module should ask one thing |
| Report | Output meant to be exported, printed or shared outside the product | Fixed measure, page break aware | Interactive controls that can't survive a PDF |

Each structure above is rendered in full under **Patterns** — this page
documents the shape and when to reach for it, not the pixels.

## Form grids

Single column is the default for any form under ~5 fields, since top-to-bottom
scanning beats side-by-side for short forms. Two-column layouts are reserved
for related-pair fields only (e.g. From/To range) — never two unrelated
fields side by side purely to save vertical space. Inline toolbars (filter
rows) sit in a single wrapping flex row with a fixed minimum width each.

| Pattern | Behavior |
|---|---|
| Single column | Default for any form under ~5 fields — top-to-bottom scanning beats side-by-side for short forms |
| Two column | Related pairs only, e.g. a From/To range, a width and a height — never two unrelated fields side by side just to shorten the page |
| Inline toolbar | Filter rows sit in a single wrapping flex row, each field with a fixed minimum width so wrapping stays predictable |
| Data grid | A table spans its container and scrolls horizontally within it, so one table with many columns never widens the viewport — the outer page never gets a horizontal scrollbar, only the table does |

<div style={{ display: 'grid', gridTemplateColumns: 'repeat(auto-fit,minmax(260px,1fr))', gap: '16px', margin: '16px 0' }}>
  <div style={{ border: '1px solid var(--color-success)', background: 'var(--color-success-surface)', borderRadius: '8px', padding: '14px' }}>
    <div style={{ fontSize: '11px', fontWeight: '700', color: 'var(--color-success-hover)', marginBottom: '8px' }}>✓ DO</div>
    <div style={{ display: 'flex', gap: '12px' }}>
      <div style={{ flex: 1 }}><div style={{ fontSize: '10px', color: 'var(--color-ink-500)' }}>Window start</div><div style={{ fontFamily: 'var(--font-mono)', fontSize: '12px', color: 'var(--color-ink-900)' }}>2026-07-01</div></div>
      <div style={{ flex: 1 }}><div style={{ fontSize: '10px', color: 'var(--color-ink-500)' }}>Window end</div><div style={{ fontFamily: 'var(--font-mono)', fontSize: '12px', color: 'var(--color-ink-900)' }}>2026-07-31</div></div>
    </div>
    <p style={{ fontSize: '12px', color: 'var(--color-ink-600)', margin: '10px 0 0' }}>Pair fields that belong to one concept.</p>
  </div>
  <div style={{ border: '1px solid var(--color-error)', background: 'var(--color-error-surface)', borderRadius: '8px', padding: '14px' }}>
    <div style={{ fontSize: '11px', fontWeight: '700', color: 'var(--color-error-text)', marginBottom: '8px' }}>✗ DON'T</div>
    <div style={{ display: 'flex', gap: '12px' }}>
      <div style={{ flex: 1 }}><div style={{ fontSize: '10px', color: 'var(--color-ink-500)' }}>Tool name</div><div style={{ fontFamily: 'var(--font-mono)', fontSize: '12px', color: 'var(--color-ink-900)' }}>SP7-04</div></div>
      <div style={{ flex: 1 }}><div style={{ fontSize: '10px', color: 'var(--color-ink-500)' }}>Notification email</div><div style={{ fontFamily: 'var(--font-mono)', fontSize: '12px', color: 'var(--color-ink-900)' }}>fab12-ops@kla.com</div></div>
    </div>
    <p style={{ fontSize: '12px', color: 'var(--color-ink-600)', margin: '10px 0 0' }}>Pack unrelated fields into a row just to shorten the page.</p>
  </div>
</div>

## Safe areas & alignment

| ✅ Do | ❌ Don't |
|---|---|
| Keep 24px clear around any fixed overlay (toast stack, modal) so it never touches the viewport edge on any supported width | Center content by eyeballing margins. Align to the column grid even inside a card — off-grid alignment is the fastest way to make a page look unintentional |

## Usage guidelines

| Rule | Meaning |
|---|---|
| Start fluid | Design the standard breakpoint first, then verify compact and wide — not the reverse |
| Whole columns only | No component spans a fractional column; if it doesn't fit cleanly, the grid should flex, not the component |
| One gutter value | 24px everywhere. A tighter gutter inside a dense table toolbar uses `sp-3`/`sp-2` spacing tokens, never a fractional gutter |
